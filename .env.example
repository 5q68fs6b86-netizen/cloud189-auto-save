# 天翼云盘接口在双栈网络下可能因 IPv4 / IPv6 切换触发 InvalidSessionKey
# 可选值: auto / ipv4 / ipv6，推荐优先使用 ipv4
DNS_LOOKUP_IP_VERSION=ipv4

# 可选：允许通过非本机地址完成首次管理员初始化。请使用高熵随机值。
INITIAL_SETUP_TOKEN=

# 可选：影巢请求流控。开启后所有影巢出站请求串行，且相邻请求至少间隔指定毫秒
HDHIVE_FLOW_CONTROL_ENABLED=false
HDHIVE_FLOW_CONTROL_MIN_INTERVAL_MS=1000
